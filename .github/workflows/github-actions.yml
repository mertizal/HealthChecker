name: github-actions
run-name: ${{ github.actor }} is learning GitHub Actions

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

  workflow_dispatch:

jobs:
  hello:
    runs_on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: hello world
        run: echo "selam dünya ben masaka"
        shell: bash
  goodbye:
    runs_on: ubuntu-latest
    steps:
      - name: goodbye world
        run: echo "Goodbye world"
        shell : bash

